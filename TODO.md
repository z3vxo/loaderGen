# TODO

- [ ] Start CLI tool (ldrgen.py) — generate config blobs + compile flags
- [ ] Fix config parser — wire ParseConfig to use g_ldr->apis, match new config format
- [ ] Add local memory: module stomp
- [ ] Add local execution: CreateFiber
